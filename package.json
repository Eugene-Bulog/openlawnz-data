{
	"name": "openlawnz-data",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"postinstall":
			"cd tests && yarn install && cd ../pdfToDBProcessor && yarn install && cd ../controller && yarn install",
		"start": "cd controller && node index"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"aws-sdk": "^2.238.1",
		"dotenv": "^4.0.0",
		"mysql": "^2.15.0",
		"request": "^2.86.0"
	}
}
